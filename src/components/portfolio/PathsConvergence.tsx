/** Ponte visivo continuo fra i due percorsi e la sezione progetti. */
export function PathsConvergence() {
  return <div className="paths-convergence-bridge relative h-56 sm:h-64" aria-hidden="true" />;
}
